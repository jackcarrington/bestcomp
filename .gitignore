# CRITICAL SECURITY: Environment files
.env
.env.local
.env.production
.env.staging
.env.development
# Note: .env.example is NOT ignored - it should be committed to help other developers

# Dependencies
node_modules/
*.tar.gz

# Build outputs and cache
dist/
.astro/
.cache/

# Build artifacts in root (source files are in src/)
/*.html
!public/*.html

# Bundle analyzer output (development only)
stats.html

# Netlify build artifacts
.netlify/

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Claude Code temporary files
.claude/
modelcontextprotocol/

# MCP server artifacts
.playwright-mcp/
.puppeteer-mcp/

# OS generated files
Thumbs.db
ehthumbs.db

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output/

# Dependency directories
jspm_packages/

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache (https://parceljs.org/)
.parcel-cache/

# Temporary folders
tmp/
temp/

# Development and deployment files
deployment.log

# Test and setup scripts (development only)
test-*.js
setup-*.js
seed-*.ts

# Documentation files now organized in docs/ folder
# Keep CLAUDE.md and README.md in root
# audit/ folder can be removed if empty

# Favicon files (keep only favicon.ico)
favicon.jpg
favicon.png


# Legacy app directory (if not in use)
# app/

# Legacy server directory (if not in use)
server/

# Root-level shared directory (not src/shared which is ACTIVE)
/shared/

# Legacy lib directory (if not in use) - NOTE: src/lib is ACTIVE
# lib/

# Root-level types directory (not src/types)
/types/

# Legacy middleware file
middleware.ts